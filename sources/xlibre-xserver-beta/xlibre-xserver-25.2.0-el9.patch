diff --git a/os/osdep.h b/os/osdep.h
index 631ebf8cf..b764c0ccc 100644
--- a/os/osdep.h
+++ b/os/osdep.h
@@ -53,6 +53,7 @@ SOFTWARE.
 #include <errno.h>
 #include <limits.h>
 #include <signal.h>
+#include <sys/random.h>
 #include <stddef.h>
 #include <X11/Xos.h>
 #include <X11/Xmd.h>
@@ -90,7 +91,7 @@ static inline void arc4random_buf(void *buf, size_t nbytes)
 {
 #ifdef HAVE_GETRANDOM
     ssize_t pos = 0;
-    while (pos < len) {
+    while (pos < nbytes) {
         ssize_t ret = getrandom(buf + pos, nbytes - pos, 0);
         if (ret <= 0) {
             if (ret < 0 && errno == EINTR)
